{
  "name": "@effectforge/presets",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Production-ready EffectForge effect presets",
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.build.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@effectforge/core": "workspace:*",
    "@effectforge/project-format": "workspace:*",
    "@effectforge/schema": "workspace:*",
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
