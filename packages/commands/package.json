{
  "name": "@effectforge/commands",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "EffectForge command system for deterministic project mutations",
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.build.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@effectforge/core": "workspace:*",
    "@effectforge/schema": "workspace:*",
    "immer": "^10.1.3",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
