{
  "name": "@effectforge/particles",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "EffectForge custom particle engine — SoA store, emitters, behaviors",
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.build.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@effectforge/core": "workspace:*",
    "@effectforge/schema": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
